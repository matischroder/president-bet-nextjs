import React from "react";
import Votation from "./prediction/Votation";


const Home = () => {
    return (
        <div className={`h-full w-full`}>
            <Votation />
        </div>

    );
}

export default Home;
